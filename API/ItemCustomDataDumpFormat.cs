namespace DrakesWorkshopLibs.API;

public enum ItemCustomDataDumpFormat
{
    /// <summary>Human-readable lines grouped by mod (default).</summary>
    Neat,
    /// <summary>Single JSON object: key → display value.</summary>
    Json,
}
